import { Button } from "@material-ui/core";
import React from "react";
import "./test.scss";
const Test = () => {
    return (
        <div className="test">
            <Button variant="contained" color="primary">
                Hello World
            </Button>
        </div>
    );
};

export default Test;
